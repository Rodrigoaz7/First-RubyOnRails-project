<!DOCTYPE html>
<html>
  <head>
    <title>Sistema de casas de shows</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<%= nav_bar brand: "Início", brand_link: root_url do %>  <!-- Acessa meu link root -->
      <%= form_tag bandas_busca_path, method: :get, class: "navbar-form navbar-right" do %>
        <div class="form-inline">
          <!-- nil signifca que que a o form nao eh baseado em nenhum objeto -->
          <div class="form-group"><%= text_field_tag :nome, @nome_busca %></div>
          <div class="form-group"><%= button_to "Buscar", nil, class: "btn btn-default" %></div>
        </div>
      <% end %>
    <% end %>

  	<div class="container">
      	<%= yield %>
    </div>
  </body>
</html>
